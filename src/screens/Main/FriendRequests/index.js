import { View, Text } from "react-native";
import React from "react";

// TODO: add chat document and references to each user and
// set id of chat document manually with uid and add id attribute

export default function FriendRequests() {
  return (
    <View>
      <Text>FriendRequests</Text>
    </View>
  );
}
