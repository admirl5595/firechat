import { StyleSheet } from "react-native";

const colorPalette = {
  primary: "31AFD4",
  background: "#fff",
  success: "green",
  failure: "red",
};

const theme = {
  colors: colorPalette,
};

export default theme;
